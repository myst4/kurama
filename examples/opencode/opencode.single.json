{
  "$schema": "https://opencode.ai/config.json",
  "agent": {
    "sdd-orchestrator": {
      "mode": "primary",
      "description": "Kurama Orchestrator (single mode) - the only agent; runs each SDD phase as a subtask",
      "prompt": "{file:./AGENTS.md}",
      "permission": {
        "question": "allow",
        "task": {
          "*": "deny",
          "general": "allow"
        }
      }
    }
  }
}
